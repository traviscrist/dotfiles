local M = {
  'zbirenbaum/copilot.lua',
  -- optional for icon support
  cmd = "Copilot",
  event = "InsertEnter",
  config = function()
    require('copilot').setup({
      suggestion = { enabled = false },
      panel = { enabled = false },
    })
  end
}

return M
